# Production Environment Configuration for AfayaConekt
# Copy this to backend/.env and frontend/.env on your VPS
# Replace all placeholder values with your actual production values

# ===========================================
# BACKEND ENVIRONMENT VARIABLES
# ===========================================

# Database Configuration
# Replace with your production PostgreSQL credentials
DATABASE_URL="postgresql://afayaconekt:CHANGE_THIS_STRONG_PASSWORD@postgres:5432/afayaconekt?schema=public"

# JWT Authentication
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET="CHANGE_THIS_TO_STRONG_RANDOM_SECRET_KEY"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# MinIO / File Storage
# Change these credentials for production
MINIO_ENDPOINT="http://minio:9000"
MINIO_ACCESS_KEY="CHANGE_THIS_MINIO_ACCESS_KEY"
MINIO_SECRET_KEY="CHANGE_THIS_MINIO_SECRET_KEY"
MINIO_BUCKET_NAME="afayaconekt-media"

# Server Configuration
# Replace with your actual domain
PORT=3001
FRONTEND_URL="https://yourdomain.com"
API_URL="https://api.yourdomain.com"

# CORS
# Replace with your actual domain
CORS_ORIGIN="https://yourdomain.com"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Email Configuration
# Use your actual SMTP credentials
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT=587
EMAIL_USER="your-production-email@gmail.com"
EMAIL_PASS="your-app-specific-password"

# Logging
LOG_LEVEL="info"

# Prisma
PRISMA_LOG_LEVEL="warn"

# ===========================================
# FRONTEND ENVIRONMENT VARIABLES
# ===========================================

# API Configuration
# Replace with your actual domain
VITE_API_URL=https://api.yourdomain.com
VITE_SOCKET_URL=https://api.yourdomain.com

# Internationalization
VITE_DEFAULT_LANGUAGE=en

# Analytics (Optional)
# Add your Google Analytics ID if using
VITE_GOOGLE_ANALYTICS_ID=

# PWA
VITE_PWA_ENABLED=true

# ===========================================
# DOCKER COMPOSE ENVIRONMENT VARIABLES
# ===========================================

# PostgreSQL
POSTGRES_DB=afayaconekt
POSTGRES_USER=afayaconekt
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD

# MinIO
MINIO_ROOT_USER=CHANGE_THIS_MINIO_ACCESS_KEY
MINIO_ROOT_PASSWORD=CHANGE_THIS_MINIO_SECRET_KEY

# ===========================================
# SECURITY NOTES
# ===========================================

# 1. Generate strong passwords using: openssl rand -base64 32
# 2. Never commit this file to version control
# 3. Keep backups of this file in a secure location
# 4. Rotate secrets regularly (every 90 days recommended)
# 5. Use different credentials for each environment (dev/staging/prod)
# 6. Enable 2FA for email accounts used for notifications
# 7. Restrict database access to application containers only
# 8. Use environment-specific API keys for third-party services